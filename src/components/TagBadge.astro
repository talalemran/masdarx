---
export interface Props {
  tag: string;
  href?: string;
}

const { tag, href } = Astro.props;
---

{
  href ? (
    <a
      href={href}
      class="inline-block px-3 py-1 text-xs font-medium font-body bg-zinc-800 text-zinc-400 rounded-full hover:bg-zinc-700 hover:text-zinc-300 transition-colors no-underline"
    >
      {tag}
    </a>
  ) : (
    <span class="inline-block px-3 py-1 text-xs font-medium font-body bg-zinc-800 text-zinc-400 rounded-full">
      {tag}
    </span>
  )
}
